name: MRMS Backfill Swaths

on:
  workflow_dispatch:

jobs:
  retired:
    name: Legacy workflow retired
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: echo "Legacy multi-month MRMS backfill is retired; use the current ingestion workflows."
