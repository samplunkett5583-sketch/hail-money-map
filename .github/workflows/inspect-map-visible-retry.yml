name: Inspect Map Visible Retry

on:
  push:
    branches:
      - pr-43-storm-panel-style
    paths:
      - '.github/workflows/inspect-map-visible-retry.yml'
  workflow_dispatch:

jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Print visible map retry block
        run: |
          sed -n '30535,30615p' public/index.html
